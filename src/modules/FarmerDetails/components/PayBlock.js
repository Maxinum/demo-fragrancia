import React from 'react';

const PayBlock = () => {
    return (
        <div className="history">
            <div className="payment_label">Оплаты</div>
            <select name="" id="payment" size="10"></select>
        </div>
    );
}

export default PayBlock;